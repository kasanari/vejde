non-fluents instance0 {
	domain = counting_bandit;
	objects {
		button : {red, green};
		machine: {r_m, g_m};
	};
	non-fluents {
		PAYOUT(r_m) = 1.0;
		PAYOUT(g_m) = 1.0;
		CONNECTED(red,r_m);
		CONNECTED(green,g_m);
		LIGHT_PROB(r_m) = 0.8;
		LIGHT_PROB(g_m) = 1.0;
	};
}

instance counting_bandit_instance0 {
	domain = counting_bandit;
	non-fluents = instance0;
	max-nondef-actions = 1;
	horizon  = 10;
	discount = 1.0;
}