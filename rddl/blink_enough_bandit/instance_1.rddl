non-fluents instance1 {
	domain = blink_enough_bandit;
	objects {
		button : {red, green};
		machine: {r_m, g_m};
	};
	non-fluents {
		CASH(r_m) = 1.0;
		CASH(g_m) = 1.0;
		CONNECTED(red,r_m);
		CONNECTED(green,g_m);
		LIGHT_PROB(r_m) = 0.5;
		LIGHT_PROB(g_m) = 0.5;
	};
}

instance blink_enough_bandit_instance1 {
	domain = blink_enough_bandit;
	non-fluents = instance0;
	max-nondef-actions = 1;
	horizon  = 30;
	discount = 1.0;
}